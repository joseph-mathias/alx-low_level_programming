#include <stdio.h>

/**
 * main - Prints all decimal numbers
 * Return: Exit status is zero means success
 */
int main(void)
{
	for(int i = 0; i < 10; i++)
	{
		printf("%d", i);
	}
	printf("\n");
	return(0);
}
