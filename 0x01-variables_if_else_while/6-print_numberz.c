#include <stdio.h>

/**
 * main - Prints all decimal digits
 * without using printf
 * Return: Exit status is zero means success
 */
int main(void)
{
	int i;
	
	for (i = 0; i = 10; i++)
		patchar(1);
	patchar('\n');

	return (0);
}
